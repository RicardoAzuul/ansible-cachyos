---
- name: Configure CachyOS laptop
  hosts: cachyos_laptop
  gather_facts: true
  vars_files:
    - ../vars/packages.yml

  tasks:
    - name: Install official CachyOS packages
      become: true
      community.general.pacman:
        update_cache: true
        name: "{{ cachyos_pacman_packages }}"
        state: present

    - name: Uninstall python-pip
      become: true
      community.general.pacman:
        name: python-pip
        state: absent

    - name: Create AUR builder user
      become: true
      ansible.builtin.user:
        name: aurbuilder
        groups: wheel
        create_home: true

    - name: Allow aurbuilder to use pacman and paru without password
      become: true
      ansible.builtin.copy:
        dest: /etc/sudoers.d/11-aurbuilder
        content: |
          aurbuilder ALL=(ALL) NOPASSWD: /usr/bin/pacman
          aurbuilder ALL=(ALL) NOPASSWD: /usr/bin/paru
        mode: '0440'
        validate: 'visudo -cf %s'

    - name: Check if paru is installed
      ansible.builtin.command: which paru
      register: paru_check
      changed_when: false
      failed_when: false

    - name: Install paru AUR helper
      when: paru_check.rc != 0
      become: true
      become_user: aurbuilder
      block:
        - name: Clone paru repository
          ansible.builtin.git:
            repo: https://aur.archlinux.org/paru.git
            dest: /tmp/paru-bin
            update: false

        - name: Build and install paru
          ansible.builtin.command: makepkg -si --noconfirm
          args:
            chdir: /tmp/paru-bin
          changed_when: true

    - name: Install AUR packages with paru
      become: true
      become_user: aurbuilder
      kewlfft.aur.aur:
        name: "{{ cachyos_aur_packages }}"
        use: paru
        state: present


    - name: Ensure ~/GoogleDrive exists for rclone
      ansible.builtin.file:
        path: "{{ ansible_user_dir }}/GoogleDrive"
        state: directory
        mode: '0755'

    - name: Ensure ~/opt exists for Espanso AppImage
      ansible.builtin.file:
        path: "{{ ansible_user_dir }}/opt"
        state: directory
        mode: '0755'

    - name: Download Espanso AppImage
      ansible.builtin.get_url:
        url: https://github.com/espanso/espanso/releases/latest/download/Espanso-X11.AppImage
        dest: "{{ ansible_user_dir }}/opt/Espanso.AppImage"
        mode: '0755'
        force: false

    - name: Register Espanso command alias
      become: true
      ansible.builtin.command: "{{ ansible_user_dir }}/opt/Espanso.AppImage env-path register"
      args:
        creates: /usr/local/bin/espanso

    - name: Register Espanso systemd user service
      ansible.builtin.command: /usr/local/bin/espanso service register
      args:
        creates: "{{ ansible_user_dir }}/.config/systemd/user/espanso.service"

    - name: Start Espanso
      ansible.builtin.command: /usr/local/bin/espanso start
      register: espanso_start
      changed_when: "'already running' not in (((espanso_start.stdout | default('')) ~ (espanso_start.stderr | default(''))) | lower)"
      failed_when:
        - espanso_start.rc != 0
        - "'already running' not in (((espanso_start.stdout | default('')) ~ (espanso_start.stderr | default(''))) | lower)"

    # - name: Check whether Homebrew is available
    #   ansible.builtin.stat:
    #     path: /home/linuxbrew/.linuxbrew/bin/brew
    #   register: brew_binary
    #
    # - name: Install Homebrew
    #   ansible.builtin.shell:
    #     cmd: NONINTERACTIVE=1 /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    #     creates: /home/linuxbrew/.linuxbrew/bin/brew
    #   register: homebrew_install
    #   when: not brew_binary.stat.exists
    #
    # - name: Verify Homebrew binary is present after installation
    #   ansible.builtin.stat:
    #     path: /home/linuxbrew/.linuxbrew/bin/brew
    #   register: brew_binary_check
    #
    # - name: Fail when Homebrew installation did not produce a brew binary
    #   ansible.builtin.fail:
    #     msg: Homebrew installation failed. The brew binary was not found at /home/linuxbrew/.linuxbrew/bin/brew.
    #   when: not brew_binary_check.stat.exists
    #
    # - name: Tap hashicorp/tap Homebrew repository
    #   community.general.homebrew_tap:
    #     name: hashicorp/tap
    #     path: /home/linuxbrew/.linuxbrew/bin
    #
    # - name: Install Vagrant via Homebrew
    #   community.general.homebrew:
    #     name: hashicorp/tap/vagrant
    #     state: present
    #     path: /home/linuxbrew/.linuxbrew/bin
